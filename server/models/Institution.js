export default class Institution {
  constructor(institution) {
    this.name = institution.name;
    this.city = institution.city;
    this.province_territory = institution.province_territory;
    this.id = institution.id;
  }
}
